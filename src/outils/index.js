const isEmpty = require("./isEmpty")
const hash = require("./hash")
const isEmail = require("./isEmail")
const isPassword = require("./isPassword")

module.exports = {
   isEmail, isEmpty, isPassword, hash
}